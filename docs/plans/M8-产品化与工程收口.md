# M8：产品化与工程收口执行清单

> 里程碑状态：待开始
> 前置里程碑：M7-Q04
> 计划粒度：9 个完整工作包；每个任务可以包含多个模块和内部检查项，不再为 Controller、Repository、页面或脚本单独编号

## 1. 目标

M8 将 M0–M7 已完成的 Team Beta 能力收口为更容易配置、更容易部署、更容易维护和更适合公开发行的产品版本。

成员完成注册和首个 Team 创建后，可以通过统一 Setup Center 看清当前 Team 距离“可执行个人任务”“可执行 Coding 任务”“可进行团队交付”还缺少哪些事实，并从同一入口完成必要配置。维护者可以通过明确的依赖、配置、质量和发布门禁持续演进项目；部署者可以直接消费官方不可变镜像，并获得真实可用的 Trace、告警和自动备份能力。

M8 不扩展新的业务场景。Conversation、WorkItem、Coding、Review、GitHub Draft PR、飞书通知和 Team Observer 继续使用既有领域合同。

## 2. 出口结果

M8 完成时形成以下结果：

1. Team Setup Readiness 成为服务端权威派生视图，统一表达模型、Agent、WorkProject、Repository、Runtime 和可选协作集成的状态；
2. Web 提供 Setup Center 和首页就绪摘要，新用户不需要在多个错误页之间猜测配置顺序；
3. 核心超大文件和运行时 Port 按稳定职责收口，替代运行时 `UnsupportedOperationException` 的隐式实现缺口；
4. Maven 依赖、Spring 配置元数据、环境变量示例和本地开发边界保持可自动验证的一致性；
5. Git Tag/Release 可以发布 Backend/Web 同 Revision 的 GHCR 镜像、Digest Manifest、SBOM、Provenance 和签名；
6. Team Beta 的 Trace 有真实去向，Prometheus 有可执行告警，备份有自动调度，TLS 和本地 Compose 完成安全收口；
7. 前端 Coverage 覆盖核心业务代码，CI 按变更范围分层执行并保留完整 Release Gate；
8. M8 Release Gate 通过真实空环境 Setup、完整任务闭环、升级恢复和公开发行验证。

## 3. 范围与非目标

### 3.1 本期范围

- Team Setup Readiness 只读取现有权威事实，不复制模型、Agent、Provider 或 WorkProject 状态；
- 必需能力与可选能力分开表达。基础对话、普通 WorkItem、Coding/GitHub、飞书通知拥有不同就绪条件；
- Setup Center 只调用既有受权命令或进入既有管理页面，不提供绕过权限的一键配置后门；
- 架构重构保持公开 API、数据库事实、事件版本、幂等和恢复语义兼容；
- 正式发行以单机 Team Beta、Linux amd64 和 GHCR 为首个支持边界；
- 质量门禁同时覆盖 Java、Vue、配置、容器、文档和发行产物。

### 3.2 非目标

- Kubernetes 高可用执行拓扑；
- 多 Organization SaaS 控制面；
- OIDC/SAML 企业身份、MFA 和 SCIM；
- Vault/KMS 高可用凭证服务；
- Plugin 市场、任意第三方代码动态装载和 Provider 商店；
- PR 自动合并、生产发布和 Autopilot；
- Desktop 与 Mobile 客户端。

## 4. 核心决策

### 4.1 Readiness 是派生事实

Setup Readiness 不建立第二套业务状态。Application Query 在当前 Organization、Team、Member 和权限范围内读取现有 ModelConnection、AgentTemplate、AgentConfiguration、WorkProject、RepositoryBinding、Provider Connection 与 Runtime Health，生成带版本的公开快照。

单项状态使用闭合集合：

```text
READY | ACTION_REQUIRED | BLOCKED | UNAVAILABLE
```

每项同时标识 `required`、安全原因码、当前成员是否有处理权限和受信任的站内 `actionKey`。响应不返回 Credential、Endpoint、Provider 原始错误、内部数据库坐标或任意跳转 URL。

### 4.2 按能力判断就绪

Setup Center 不使用一个虚构的总百分比。它分别回答：

- 是否可以开始 Personal Conversation；
- 是否可以创建和执行普通团队任务；
- 是否可以执行 Coding/Review；
- 是否可以创建 GitHub Draft PR；
- 是否可以使用飞书通知和 Team Observer。

可选集成缺失不能阻断基础对话。没有 GitHub 时仍可执行非代码任务；没有飞书时仍可使用站内 Inbox 和 Activity。

### 4.3 重构保持行为兼容

大文件按稳定职责拆分，先建立 Characterization/Contract Test，再移动实现。M8 不借重构修改领域语义、数据库历史迁移、事件 Schema、权限策略或公开错误合同。

### 4.4 PR Gate 与 Release Gate 分层

Pull Request 优先提供快速反馈；Main 保留完整回归；Release 额外验证不可变镜像、签名、安装、备份恢复和真实浏览器闭环。路径过滤不能跳过与变更有关的门禁，Release Gate 继续要求所有必需证据存在且成功。

## 5. 依赖顺序

```text
M7-Q04 -> M8-S01

M8-S01 -> M8-A01 -> M8-F01
M8-S01 -> M8-E01
M8-S01 -> M8-I01 -> M8-I02
M8-S01 -> M8-I03

M8-A01,M8-F01,M8-E01,M8-I01 -> M8-Q01
M8-I02,M8-I03,M8-Q01 -> M8-Q02
```

## 6. 执行任务

| ID | 类型 | 依赖 | 涉及模块 | 完整交付结果 | 主要验收 |
|---|---|---|---|---|---|
| `M8-S01` | SPIKE | M7-Q04 | product/application/server/web/ci/deploy | 冻结 Team Setup Readiness、能力级就绪、受信 `actionKey`、配置来源、PR/Main/Release 三层门禁和正式发行坐标；记录现有性能、Coverage、依赖、镜像、Trace 与运维基线 | 使用新 Team、已配置 Team、无权限成员、Provider 故障和部分配置五类 Fixture 证明状态闭合、无敏感字段、无虚构进度；形成被接受的 ADR 或等价设计记录 |
| `M8-A01` | FEATURE | S01 | domain/application/infrastructure/server/docs | 实现只读 Team Setup Readiness Query、能力评估、权限裁决和公开 API；聚合模型、Agent、WorkProject、Repository、Runtime 与可选集成事实，复用现有命令/API 作为后续动作 | PostgreSQL/Redis 集成与 API 契约覆盖跨 Team、撤权、部分故障、并发配置变化、稳定原因码、缓存/ETag 或等价版本边界、查询预算和公开字段白名单 |
| `M8-F01` | FEATURE | A01 | web/docs | 交付 `/setup` Setup Center、Today 就绪摘要、Onboarding 完成后的下一步入口，并统一模型、Agent、WorkProject、Repository 和协作集成空状态；具备权限的成员可继续配置，无权限成员看到明确责任人提示 | Desktop/390px、键盘、Axe、视觉、离线、部分失败、权限不足和 Team 切换通过；从空 Team 到 Personal Conversation、Coding Preflight 和可选 GitHub/飞书配置均有可发现路径 |
| `M8-E01` | TASK | S01 | application/agentscope/infrastructure/server/web | 对最重的职责边界做行为兼容重构：WorkPage/Task Store、Notification Intent Projection、Review Persistence、Operations API、AgentScope Task Runtime；拆出编排、查询、映射、SQL、DTO、状态和控制职责，并将必需 Port 能力改为编译期或 Contract Test 可验证 | 重构前后公开 API、事件、数据库、运行时恢复和前端行为一致；生产 Adapter Contract Test 覆盖必需方法；核心类职责和依赖方向有自动化边界检查，不以机械行数作为唯一完成标准 |
| `M8-I01` | TASK | S01 | all-java/web/config/docs | 收口 Maven 直接依赖、移除默认包中的未使用 Kubernetes/Fabric8 运行依赖、生成 Spring Configuration Metadata、建立环境变量/Secret/README 漂移检查，并把本地 PostgreSQL/Redis 绑定到 Loopback | `dependency:analyze` 经白名单后无未解释的 used-undeclared；默认 Backend Jar 不包含 Kubernetes Client；IDE 可读取配置元数据；配置合同和本地 Compose 安全检查进入 CI |
| `M8-I02` | FEATURE | I01 | ci/deploy/docs | 建立 `v0.1.0-beta.1` 及后续版本的正式发行流水线，发布 Backend/Web 同 Revision GHCR 镜像、不可变 Digest Manifest、SBOM、Provenance 和签名；补齐 Release Notes、升级说明及开源贡献/安全文档 | 受保护 Tag 从干净源码生成并扫描产物；全新 Linux amd64 主机只使用发行坐标完成安装；Digest、Git Revision、Schema 和前端版本一致；篡改或缺失签名/证据时失败关闭 |
| `M8-I03` | FEATURE | S01 | deploy/observability/security/docs | 完成 Team Beta 运维收口：Trace 输出到可查询 Backend 或显式关闭，提供 Prometheus 告警与通知出口、自动 Daily/Weekly 备份和 Retention、备份年龄监控、HSTS/CSP/TLS 模板以及运维验证手册 | 模拟 API/Worker 不可用、Outbox/Dead Letter 积压、通知失败、Trace 查询、备份失败/过期和空目标恢复；告警可达、Trace 可查、定时任务可重复安装，公网只开放 80/443 |
| `M8-Q01` | HARDENING | A01,F01,E01,I01 | all/ci | 重建质量反馈面：前端 Coverage 默认纳入全部生产业务代码，增加低噪音格式/Lint/架构/依赖门禁；CI 启用并发取消、路径感知和 Ubuntu/macOS 分工，README 的历史数字绑定 Revision | 核心 Conversation、Task、Coding、Review、Delivery、TeamOps 和 TeamObserver 进入 Coverage 分母；文档变更不触发无关完整构建；代码变更仍覆盖必要门禁；完整 Main Gate 与现有固定攻击/故障集零回归 |
| `M8-Q02` | HARDENING | I02,I03,Q01 | all/release | 聚合 M8 Release Gate，在真实 PostgreSQL/Redis、生产 Web、正式 GHCR 镜像和 Linux amd64 上完成空环境 Setup、双用户协作、Coding/Review/Draft PR、升级恢复、观测告警与备份恢复验证 | 所有必需任务与证据成功；从注册到首个有效执行不依赖手工改库；发行镜像可验证、可部署、可观测、可备份、可恢复；形成 M8 Release 决定与固定证据索引 |

## 7. 建议执行波次

| 波次 | 任务 | 可演示结果 |
|---|---|---|
| W1 产品合同 | S01 | Setup Readiness、发行和质量边界冻结 |
| W2 配置闭环 | A01、F01 | 新 Team 可以从统一 Setup Center 走到可执行状态 |
| W3 工程与生产收口 | E01、I01、I02、I03 | 核心职责、依赖、配置、发行和运维形成稳定合同 |
| W4 质量与发布 | Q01、Q02 | 分层 CI 与 M8 正式 Release Gate 通过 |

W2 与 W3 可以在 S01 完成后并行。E01 的内部重构按文件逐个合并，但仍以一个行为兼容工作包验收；I02 可以先完成发行 Dry Run，最终签名发行在 Q02 关闭。

## 8. M8 Release Gate

M8 完成需要同时满足：

1. 新注册用户创建首个 Team 后可以从 Setup Center 看清必需项、可选项、处理权限和下一步；
2. 基础对话不被 GitHub/飞书等可选集成阻断，Coding、Review 和 Draft PR 只在各自权威前置条件满足时进入 READY；
3. Readiness 不复制领域状态，不暴露 Secret、Credential、Endpoint、Provider 原始错误或任意 URL；
4. 核心职责重构不改变现有 API、事件、数据库、幂等、权限和恢复语义；
5. 默认构建不携带未使用 Kubernetes Client，直接依赖和配置元数据通过自动化门禁；
6. 前端核心业务代码进入 Coverage 分母，Lint、格式、架构和依赖门禁低噪音且可持续执行；
7. PR、Main 和 Release 三层 CI 反馈范围明确，取消旧运行且不会因路径过滤漏掉相关验证；
8. 官方 Tag 发布 Backend/Web 同 Revision 的已扫描、可验证、固定 Digest 镜像和发行证据；
9. Trace 可查询或被明确关闭，关键故障产生可达告警，备份自动运行且空目标恢复成功；
10. 本地 Compose、TLS 模板、公开发行和开源协作文档完成安全与可用性收口；
11. 真实 Linux amd64 环境从正式发行产物完成空环境 Setup、双用户协作和完整研发任务闭环；
12. M0–M7 全量固定攻击、故障、恢复、浏览器和迁移门禁零回归。

## 9. 任务规模约定

M8 的编号代表可以独立验收的工作包，不代表一次提交。每个工作包允许包含若干内部 Checklist、多个小提交和必要的测试/文档更新；内部 Checklist 不再继续生成新的 M8 编号。只有当任务出现独立的产品出口、不同责任人或无法共同回滚的风险边界时，才新增任务编号。
