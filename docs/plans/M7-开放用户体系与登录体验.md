# M7：开放用户体系与登录体验执行清单

> 对应总计划：[CrewScope 实施计划](../CrewScope-实施计划.md) M7<br>
> 前置条件：M6-Q04 MVP Release Gate 通过，Principal、TeamMember、TeamRole、Redis、审计、部署与双入口前端基线稳定<br>
> 目标周期：3–4 周，按“认证内核 → 账号与会话 → Onboarding/邀请 → 前端闭环 → 安全门禁”纵向推进<br>
> 目标结果：用户通过 CrewScope 自有登录与注册页面进入平台，拥有独立账号和服务端会话，可创建第一个 Team、获得默认 Personal Agent、邀请其他用户并以各自身份协作；浏览器不再出现 HTTP Basic 原生认证弹窗<br>
> 当前进度：`M7-S01` 至 `M7-S04`、`M7-D01` 至 `M7-D08`、`M7-I01` 至 `M7-I08`、`M7-A01` 至 `M7-A07`、`M7-F01` 至 `M7-F08`、`M7-Q01` 至 `M7-Q04` 已完成，M7 全部完成

## 1. 出口结果与范围

M7 完成后具备：

- CrewScope 本地 `UserAccount`、可扩展 `LoginIdentity`、`LocalCredential` 与 Organization Binding；
- `OPEN / INVITE_ONLY / DISABLED` 三种注册模式及部署级安全默认值；
- 基于 Spring Security、Spring Session Data Redis 和服务端 Cookie 的登录会话；
- 正式 `/login`、`/register`、`/onboarding`、`/account` 与 `/invite#token=...` 页面；
- 注册、登录、退出、当前会话、资料修改、密码修改、首次 Team 创建和邀请接受 API；
- 新账号创建第一个 Team 后自动成为 Owner，并复用 M1 事务创建默认 Workspace、内置角色和 Personal Agent；
- Team Owner/Admin 创建、查看和撤销一次性邀请链接，已登录或新注册用户均可接受；
- Bootstrap Operator 迁移到相同登录页，人类登录凭证与 Prometheus 监控抓取凭证分离，浏览器业务入口不再启用 HTTP Basic Challenge；
- 密码哈希、登录限流、临时锁定、Session 固定防护、CSRF、Cookie 和账号枚举防护；
- 两个真实独立用户完成“注册—建 Team—邀请—加入—协作—退出—重新登录”的纵向 E2E。

M7 的 `OPEN` 注册面向单 Organization 自托管实例，不宣称公共多租户 SaaS 隔离。M7 不包含企业 OIDC/LDAP/SCIM 接入、飞书/GitHub 社交登录、邮件发送、邮箱验证、忘记密码邮件、MFA、Passkey、跨部署统一账号、跨 Organization 切换、平台级计费、账号合并、管理员冒充登录或公开 SaaS 反滥用运营系统。`LoginIdentity` 和 Account/Principal 分层为这些能力保留扩展边界，但不提前实现外部身份流程。

## 2. 产品闭环

```text
访问 CrewScope
  -> Session Store 查询当前账号
  -> 未登录进入 /login
  -> 用户登录，或在 OPEN 模式进入 /register
  -> 服务端验证 Credential 并旋转 Session ID
  -> 当前账号没有 ACTIVE Team 时进入 /onboarding
  -> 创建第一个 Team
  -> 原子创建 Owner Membership、默认 Workspace、内置 Role Grant 与 Personal Agent
  -> 进入 Conversation / Control 双入口
  -> Owner 创建一次性邀请链接
  -> 第二个用户登录或注册并接受邀请
  -> 第二个用户获得 MEMBER Grant 与默认 Personal Agent
  -> 两个用户以不同 Principal 协作，权限与 Audit 可区分
  -> 退出登录使服务端 Session 失效并返回 /login
```

## 3. 核心决策

### 3.1 Account、LoginIdentity 与 Principal 分层

- `UserAccount` 是平台登录主体，保存用户名、规范邮箱、展示名、状态、安全版本和 `USER / OPERATOR` 平台角色；自助注册始终为 `USER`；
- `LoginIdentity` 保存登录提供方与稳定 Subject。M7 只实现 `local`，Subject 使用不可变 Account ID，不使用可修改邮箱；
- `LocalCredential` 只保存强密码哈希、算法标识、密码版本和修改时间，不进入 DomainEvent、Audit、日志或查询 DTO；
- `AccountOrganizationBinding` 显式允许 Account 进入 Organization，并绑定该 Organization 内的 USER Principal；Binding 单向引用 Account 与 Principal，Principal 不反向依赖 Account，避免身份模型循环；
- `Principal` 继续作为 Organization 内用户、Agent 和 Service 的统一业务 Actor；TeamMember 与 TeamRole 不感知密码和 Session；
- 新本地 Account 创建的 USER Principal 不再把 `principal.identity_provider/external_subject` 作为登录真相；旧 Bootstrap/OIDC Principal 的 ExternalIdentity 作为兼容数据保留，登录映射的唯一权威来源是 LoginIdentity 与 Binding；
- M7 的本地部署默认绑定一个 Bootstrap Organization，同一实例中的 Account 只会话化一个 ACTIVE Binding。数据库仍使用 `Account + Organization` 约束，不把 Account 与单一 Organization 永久耦合；多 Organization 选择和企业 Issuer Binding 延后。

### 3.2 服务端 Session

- 浏览器认证使用服务端 Session，不向前端签发长期 JWT，不把 Token 保存到 `localStorage`；
- Spring Security Context 保存到 Spring Session Data Redis，Cookie 使用 `HttpOnly`、部署环境 `Secure`、`SameSite=Lax` 和受控 Path；
- 登录成功必须旋转 Session ID，退出必须使服务端 Session 失效并清理 Cookie；
- `GET /api/v1/auth/session` 对匿名请求返回 `authenticated=false`、Registration Mode 与 CSRF Header/Token 公开坐标，对已登录请求追加 Account、Principal、Organization、Team 摘要和权限；防 CSRF Token 是同源前端需要回传的公开值，只出现在受控 CSRF Cookie、Session 公开投影和写请求 Header，不进入 `localStorage`、`sessionStorage`、IndexedDB 或 Telemetry；
- 所有浏览器写请求都使用 Cookie CSRF Token 与同源校验；登录、注册和邀请接受同时使用独立的未认证限流与一次性校验边界；
- Redis 短暂不可用时认证失败关闭，不回退到客户端身份或无状态超级用户。

### 3.3 开放注册与 Onboarding

- `OPEN` 允许用户自助注册；`INVITE_ONLY` 只接受有效邀请上下文；`DISABLED` 关闭新本地账号；
- 注册请求只接受用户名、邮箱、展示名、密码和可选邀请 Token，不接受 Organization、Principal、Team、Role 或权限；
- 用户名与邮箱使用确定性规范化、显示值与规范值分离，冲突响应不泄漏其他账号细节；
- 密码策略以长度、常见密码拒绝和最大输入预算为主，不使用复杂字符拼凑规则；
- 无邀请注册创建 Account、Identity、Credential、Organization Binding 和 USER Principal，由用户显式完成 Onboarding 创建 Team；带邀请注册在同一事务锁定并消费邀请、创建 Membership，不先创建一个未绑定邀请的账号；
- Team 创建复用 M1 `TeamCreationService`，禁止 M7 再实现一套 Team/Role/Personal Agent 初始化逻辑。

### 3.4 Team Invitation

- 邀请保存 Team、邀请人、可选目标邮箱、目标内置角色、过期时间、状态和 Token Hash；
- 明文 Token 只在创建成功时返回一次，数据库、日志、Audit、前端 Store 和列表响应只保存安全摘要；
- 邀请链接把 Token 放入 URL Fragment，Fragment 不发送到 Web/Nginx；前端读取到进程内存后立即清除地址栏，再通过 POST Body 执行 Preview/Accept；
- Preview 只返回 `AVAILABLE / EXPIRED / UNAVAILABLE`；可用时附带 Team 名称、目标角色、过期时间和是否定向，不返回目标邮箱、Token Digest、邀请人身份、成员目录或内部 Scope；
- Accept 在同一事务锁定邀请，验证 ACTIVE Account、Organization Binding、Team 当前状态、邀请版本和 Token Hash，再复用成员加入服务；
- 同一 Account 重复接受收敛为既有 Membership；Token 重放、撤销、过期、目标邮箱不匹配和跨 Organization 使用失败关闭；
- M7 不发送邮件，Owner 复制邀请链接交付。后续邮件/飞书通知只消费相同 Invitation 事实。

### 3.5 Bootstrap、监控身份与企业扩展

- V30 升级时使用现有 `bootstrap/crewscope-monitor` ExternalIdentity 精确查找 USER Principal；存在时创建 Account/Binding 并保留 Principal ID、TeamMember 和 Audit 历史，不存在时才创建新 Operator USER Principal；
- 旧 `bootstrap_password` 只作为人类 Bootstrap Operator 初始/轮换凭证输入，启动时不回显；Operator Account 持有 `OPERATOR` 平台角色，自助注册无法获得该角色；
- Prometheus 改用独立机器账号与 `monitoring_password` Secret，只能访问受控 Actuator 抓取路径，不创建 UserAccount、TeamMember 或业务 Session；升级 Guard 拒绝 Operator 与监控继续共用凭证；
- Web 业务入口不返回 `WWW-Authenticate: Basic`。Basic 仅可用于精确匹配的机器运维路径，其 SecurityWebFilterChain 与普通 Web/REST Session 链分离；
- 后续 OIDC 只新增 `LoginIdentity` 和认证 Adapter，将同一 Account 映射到现有 Principal，不改变 TeamMember、权限、Conversation 或 Task 授权模型。

## 4. 依赖顺序

```text
M7-S01 -> M7-I02
M7-S02 -> M7-D01 -> M7-D02
M7-S02,M7-D01 -> M7-D04
M7-S03,M7-D01,M7-D02 -> M7-D03
M7-D01,M7-D04 -> M7-D05
M7-D01..D04 -> M7-D07 -> M7-I01
M7-D01..D05 -> M7-D06
M7-D05,M7-D07 -> M7-D08 -> M7-I06
M7-I01,M7-D02,M7-D03 -> M7-I03
M7-I02,M7-D03 -> M7-I04
M7-D04,M7-I01 -> M7-I05
M7-I01,M7-I03,M7-I05 -> M7-I07
M7-D06,M7-I02..I05,M7-I07 -> M7-A02

M7-D05,M7-D06,M7-I02..I04,M7-I06 -> M7-A01
M7-A02,M7-D01..D03,M7-I01,M7-I03 -> M7-A03
M7-A02,M7-D04 -> M7-A04
M7-D05,M7-D06,M7-I06,M7-A02 -> M7-A05
M7-A01..A05,M7-I05 -> M7-A06 -> M7-A07

M7-S04 -> M7-F01
M7-F01,M7-A02,M7-A06 -> M7-F02
M7-F01,M7-A01,M7-A06 -> M7-F03
M7-A02,M7-F02,M7-F03 -> M7-F04
M7-A04,M7-F04 -> M7-F05
M7-A03,M7-F04 -> M7-F06
M7-A05,M7-F03,M7-F04 -> M7-F07
M7-F01..F07,M7-I08 -> M7-F08

认证与公开字段边界完成 -> M7-Q01
账号、迁移与并发契约完成 -> M7-Q02
完整双用户链路完成 -> M7-Q03
全部能力与部署升级完成 -> M7-Q04
```

## 5. Spike 与架构验证

| ID | 类型 | 依赖 | 涉及模块 | 实施内容 | 验证 |
|---|---|---|---|---|---|
| `M7-S01` | SPIKE | M6-Q04 | server/infrastructure/web | 验证 WebFlux Spring Security、Spring Session Data Redis、JSON 登录、SecurityContext 持久化、Session ID 旋转、Cookie CSRF 与 Vue 同源请求的最小闭环 | 已使用真实 Redis 与双浏览器上下文验证 8 KiB 登录体预算、错误密码零会话旋转、登录隔离、CSRF、退出失效、Session TTL 和 API/Web 反向代理；证据见 [M7-S01 验证记录](../spikes/M7-S01-WebFlux-Session与CSRF验证记录.md) |
| `M7-S02` | SPIKE | M0-D02,M1-D01 | domain/application/infrastructure | 冻结 UserAccount、LoginIdentity、AccountOrganizationBinding、Principal 和 TeamMember 的身份边界，以及 Bootstrap Operator 无损升级协议 | 已通过 16 路并发首次映射、同账号多身份、跨 Organization 拒绝、唯一 Binding、既有 Principal/TeamMember 无损升级和错误类型/Scope/状态 Fixture；形成 [ADR-024 草案](../adr/ADR-024-Account与Principal身份边界.md)，证据见 [M7-S02 验证记录](../spikes/M7-S02-Account与Principal边界验证记录.md) |
| `M7-S03` | SPIKE | S01 | server/infrastructure/security | 评估 Spring PasswordEncoder 的 Argon2id/BCrypt 参数、登录限流、临时锁定、账号枚举防护和密码输入预算 | 已在原生 Linux 2C2G/8C16G Profile 冻结 Argon2id M32/T3/P1、2/4 路 Permit、100 ms 准入等待、128 code point/512 byte 输入预算、两级限流与 15 分钟锁定；形成 [ADR-025 草案](../adr/ADR-025-本地密码与登录防护参数.md)，证据见 [M7-S03 验证记录](../spikes/M7-S03-密码与登录防护验证记录.md) |
| `M7-S04` | SPIKE | M6-F08 | web/product | 建立登录、注册、Onboarding、邀请和账号页的信息架构、双视口线框、焦点顺序与品牌视觉基线 | 已冻结 12 个状态、公开 AuthLayout 与 Account AppShell 边界、desktop/390px 结构、键盘焦点和零敏感持久化；Vitest 4/4、Playwright 6/6、Axe 零违规、Histoire 15 stories/116 variants，Darwin/Linux 双平台视觉基线通过；证据见 [M7-S04 验证记录](../spikes/M7-S04-开放身份体验与视觉基线验证记录.md) |

## 6. 领域、迁移与持久化契约

| ID | 类型 | 依赖 | 涉及模块 | 实施内容 | 验证 |
|---|---|---|---|---|---|
| `M7-D01` | TASK | S02 | domain/application | 已完成：实现 UserAccount、Username、NormalizedEmail、AccountStatus、PlatformRole、SecurityVersion 与状态机；邮箱和用户名变更保持显示值/规范值分离 | [M7-D01 账号领域内核](../testing/M7-D01-账号领域内核.md)；18 个测试覆盖 NFKC/NFC/IDN 规范化、code point/Unicode 安全边界、闭合状态转移、USER/OPERATOR 权限分层、业务/Security 版本和不可枚举冲突 |
| `M7-D02` | TASK | S02,D01 | domain/application | 已完成：实现 LoginIdentity、IdentityProviderKey、稳定 Subject、LocalCredentialMetadata 和每 Account/Provider 唯一约束；Credential 明文与哈希值禁止进入字符串表示 | [M7-D02 LoginIdentity 与 LocalCredential 契约](../testing/M7-D02-LoginIdentity与LocalCredential契约.md)；27 个测试覆盖同 Account 多 Provider、双唯一键、Subject 不可变/脱敏、Identity 状态、Credential Version、BCrypt→Argon2id 升级与哈希零元数据泄漏 |
| `M7-D03` | TASK | S03,D01,D02 | domain/application | 已完成：实现 RegistrationMode、PasswordPolicy、LoginAttemptPolicy、有界 AccountLoginAttemptState 和安全失败折叠；统一 `invalid_credentials / too_many_requests` 结果 | [M7-D03 注册与登录防护领域契约](../testing/M7-D03-注册与登录防护领域契约.md)；17 个测试覆盖 OPEN/INVITE_ONLY/DISABLED、密码双预算/常见值、失败滑动窗口、15 分钟锁定恢复和公开枚举一致性 |
| `M7-D04` | TASK | S02,D01 | domain/application | 已完成：实现 AccountOrganizationBinding、双唯一坐标、闭合状态机、既有 Principal 并发收敛和 Account→Organization→USER Principal 只读解析；Binding 单向引用两端，本地请求路径不具备 Principal Provision 能力 | [M7-D04 AccountOrganizationBinding 领域契约](../testing/M7-D04-AccountOrganizationBinding领域契约.md)；22 个测试覆盖状态交集、跨 Organization 拒绝、类型/Scope 冲突、无反向 Account 依赖、16 路绑定收敛和 Bootstrap Principal 原位复用 |
| `M7-D05` | TASK | D01,D04,M1-D03 | domain/application | 已完成：实现 TeamInvitation、InvitationTokenDigest、PENDING→ACCEPTED/REVOKED/EXPIRED 单向状态机、可选规范邮箱、目标内置角色和稳定 Account/Membership 接受结果；邀请接受复验完整身份与 Team 状态交集 | [M7-D05 TeamInvitation 领域契约](../testing/M7-D05-TeamInvitation领域契约.md)；24 个测试覆盖摘要脱敏、重放、精确过期、撤销、邮箱不匹配、Team/Principal/Binding/Membership 冲突、16 路并发单终态和既有成员幂等 |
| `M7-D06` | TASK | D01..D05 | domain/application | 已完成：定义账号注册、认证成功/失败聚合、临时锁定、退出、资料/密码修改、邀请创建/接受/撤销的 10 个 V1 安全 DomainEvent，并注册精确 Audit Schema、Outcome 与 EXTENDED Retention | [M7-D06 认证与邀请安全事件契约](../testing/M7-D06-认证与邀请安全事件契约.md)；13 个测试通过精确组件白名单、JSON 敏感字段扫描、固定失败分类、角色/版本边界和 Audit Summary 拒绝证明不含密码、Hash、Session ID、Cookie、CSRF、邀请 Token/Digest、完整邮箱或原始认证错误 |
| `M7-D07` | TASK | D01..D04 | infrastructure | 已完成：新增 `V31__local_user_account_and_identity.sql`，落地 Account、LoginIdentity、LocalCredential/元数据视图、Organization Binding、PlatformRole、规范唯一键、复合外键、强版本/状态/审计触发器与 Hash 权限隔离 | [M7-D07 本地账号与身份迁移](../testing/M7-D07-本地账号与身份迁移.md)；6 个专项测试覆盖空库 V1→V31、V30→V31、非默认 search_path、跨租户 FK、唯一冲突、敏感列权限、状态/版本和整笔回滚 |
| `M7-D08` | TASK | D05,D07 | infrastructure | 已完成：新增 `V32__team_invitation.sql`，落地邀请、Token Digest、有效期、目标角色、一次性接受、状态/版本强一致、接受链互斥行锁、查询索引和不含 Digest 的元数据视图；历史只追加安全事件保持独立 | [M7-D08 TeamInvitation 迁移](../testing/M7-D08-TeamInvitation迁移.md)；8 个专项测试覆盖空库 V1→V32、V31→V32、Digest 唯一、并发接受、Account/邮箱/Team 复核、并发停用阻断、过期扫描、跨 Scope FK、视图权限隔离、无明文 Token 和整笔回滚 |

## 7. 基础设施与安全适配

| ID | 类型 | 依赖 | 涉及模块 | 实施内容 | 验证 |
|---|---|---|---|---|---|
| `M7-I01` | TASK | D07 | infrastructure/application | 已完成：实现 UserAccount、LoginIdentity、只读 CredentialMetadata、AccountOrganizationBinding 的 PostgreSQL Repository Adapter，补充规范键复核、行锁、乐观锁、REQUIRED 事务边界和单查询 CurrentAccountSnapshot | [M7-I01 账号身份持久化适配器](../testing/M7-I01-账号身份持久化适配器.md)；9 个 Testcontainers 场景覆盖完整往返、规范查询/漂移拒绝、快照归属校验、锁互斥、乐观锁、并发注册、跨 Scope、事务回滚和固定一条 SQL 当前账号读取 |
| `M7-I02` | TASK | S01,D07 | server/infrastructure | 已完成：接入 Spring Session Data Redis，实现注册/登录共用的 Session 建立、ID 旋转、当前退出和账号级 LRU 上限；显式隔离 Bootstrap/Worker，冻结 Boot 4 Indexed Repository、Namespace、TTL、Jackson 3 白名单与 Redis 失败关闭 | [M7-I02 Spring Session Redis 会话边界](../testing/M7-I02-Spring-Session-Redis会话边界.md)；真实 Redis 覆盖创建、续期、旋转、退出、过期、Repository 重建、两实例共享、LRU 淘汰和 Redis 不可用失败关闭 |
| `M7-I03` | TASK | S03,D02,D03,I01 | server/infrastructure | 已完成：实现独立 LocalCredentialStore、当前 Argon2id 与批准的弱 Argon2id/BCrypt Reader、启动期 Dummy Hash、公平 100 ms 有界异步准入、关闭期排队收敛、Bootstrap 编码器隔离和双版本 CAS Rehash | [M7-I03 本地密码编码与安全升级](../testing/M7-I03-本地密码编码与安全升级.md)；10 个 PostgreSQL/安全场景覆盖正确/错误密码、旧参数升级、超长/非法输入、未知/禁用/损坏 Credential 等量 Dummy Match、并发改密、准入过载、关闭收敛和 Hash/明文零日志 |
| `M7-I04` | TASK | S03,D03,I02 | infrastructure/server | 已完成：使用版本化用途分离 HMAC 与 Cluster-safe Redis Lua，实现注册/登录隔离的规范标识/受控网络联合准入、已知账号失败计数、临时锁定、可信代理链解析和固定枚举指标 | [M7-I04 Redis 登录防护与临时锁定](../testing/M7-I04-Redis登录防护与临时锁定.md)；13 个专项场景覆盖 10/11、60/61、窗口下界、16 路并发、代理头不可信、锁定恢复、时间/结果异常、Redis 停止/重启和低基数指标 |
| `M7-I05` | TASK | D04,I01 | application/server | 已完成：将 AuthenticationSubjectExtractor 拆分为本地 Account Session 与外部稳定身份，使用当前 Account/SecurityVersion/LoginIdentity/Organization Binding 解析既有 Principal，从持久化 PlatformRole 派生 USER/OPERATOR Authority，并删除请求 Organization 首次映射 | [M7-I05 Account 与 Organization 请求身份解析](../testing/M7-I05-Account与Organization请求身份解析.md)；80 个联合回归覆盖真实 PostgreSQL/Redis、本地 Session、跨 Organization、自注册无 OPERATOR、状态撤权、Team 权限和 Task/Provider 既有入口 |
| `M7-I06` | TASK | D05,D08 | infrastructure/application | 已完成：实现 Digest-only Invitation Repository、256-bit CSPRNG Base64URL Token、用途分离 HMAC-SHA256、一次性明文返回、事务行锁、Keyset 分页和有界并行过期清理 | [M7-I06 邀请持久化与一次性 Token](../testing/M7-I06-邀请持久化与一次性Token.md)；43 个联合测试覆盖 Token 不落库、密钥配置失败关闭、摘要隔离、并发单终态领取、撤销/过期、无重复分页和清理保留邀请及 DomainEvent 事实 |
| `M7-I07` | TASK | S02,D07,I01,I03,I05 | server/infrastructure | 已完成：在 Organization 行锁与同一 REQUIRED 事务内，将旧 `bootstrap/crewscope-monitor` Principal 原位升级为 Account、Local LoginIdentity、Credential 和 Binding；OPERATOR 只可由受信工厂创建，外部 Secret 变更执行 CAS 轮换并推进 SecurityVersion，重复启动幂等 | [M7-I07 Bootstrap Operator 账号升级](../testing/M7-I07-Bootstrap-Operator账号升级.md)；真实 V30→V32、旧 Principal 存在/不存在、TeamMember/Audit ID 保持、8 路并发、Secret 轮换、冲突/错误权限、密码零回显和无 Basic Challenge 测试 |
| `M7-I08` | TASK | I02..I07 | server/deploy/observability | 已完成：更新 Team Beta 正式/Demo 配置、Compose、Secret、健康检查和 V26–V32 恢复；分离 Operator/Prometheus 凭证与 SecurityWebFilterChain，冻结 Registration Mode、HTTPS/Secure Cookie、登录防护、4/2 Hash Permit 和结构化日志 Guard，并为认证指标建立独立 64 Series 闭集 | [M7-I08 Team Beta 认证部署安全边界](../testing/M7-I08-Team-Beta认证部署安全边界.md)；54 个 Java 场景及 Compose/恢复/Shell 合同覆盖本地/HTTPS、Redis Session、密码准入、Actuator 独立抓取、Secret 分离、V30→V32、配置失败关闭和未跟踪 Secret 扫描 |

## 8. 应用、认证与 API

| ID | 类型 | 依赖 | 涉及模块 | 实施内容 | 验证 |
|---|---|---|---|---|---|
| `M7-A01` | FEATURE（已完成） | D05,D06,I02..I04,I06 | application/server | 已实现 `POST /api/v1/auth/register`，复用 I02 Session 建立边界，按 Registration Mode 在 PostgreSQL 事务内原子创建 Account、Local Identity、Credential、Organization Binding、USER Principal、事件和安全 Audit；带邀请注册同时锁定一次性 Token，在确认目标 Role 仍可授予后创建 Membership 并消费邀请，提交后再建立 Session | 已覆盖匿名防护、公开 DTO、幂等恢复、密码复验、停用 Role 失败关闭、USER 固定角色、事务/Outbox/Receipt 边界和统一公开错误；证据见 [M7-A01](../testing/M7-A01-本地账号注册与邀请事务.md) |
| `M7-A02` | FEATURE（已完成） | S01,D06,I02..I05,I07 | application/server | 已实现 JSON 登录、退出和 `GET /api/v1/auth/session`；匿名投影提供 Registration Mode 与 CSRF Header/Token 坐标，登录旋转 Session，已认证投影每次从当前 Account/Principal/Organization/Team/Role 事实重建；顶层只发布账号级权限，每个 Team 单独发布其能力 | 应用/API/安全专项与 Redis 集成覆盖 Dummy Match、限流/锁定、两浏览器、多实例、续期、退出、过期、当前 Team 权限、跨 Team 管理能力不合并、状态撤权、无 Basic Challenge 和字段白名单；证据见 [M7-A02](../testing/M7-A02-JSON登录与Session投影.md) |
| `M7-A03` | FEATURE（已完成） | A02,D01..D03,I01,I03 | application/server | 已实现当前账号资料读取/单版本多字段修改、密码修改和全部 Session 撤销；不提供可枚举其他账号的用户名/邮箱可用性 API，用户名/邮箱、改密和全部设备退出要求当前密码与 SecurityVersion | 应用/API 12 个、PostgreSQL 9 个与真实 Redis 6 个场景覆盖强 ETag、统一冲突、错误当前密码、Credential/SecurityVersion 双版本、改密后旧 Session 失效、并发保护和安全审计摘要；证据见 [M7-A03](../testing/M7-A03-当前账号资料改密与全会话撤销.md) |
| `M7-A04` | FEATURE（已完成） | A02,D04,M1-A01 | application/server | 已实现 `GET /api/v1/onboarding` 和 `POST /api/v1/onboarding/team`；使用当前 Account/SecurityVersion 复验、Account `FOR UPDATE` 锁和新的首 Team Guard，复用 M1 Team 事务与默认 Personal Agent 初始化；首 Team 使用独立 `CREATE_FIRST_TEAM` 幂等域 | 应用/API、Spring 装配与真实 PostgreSQL 双线程验证覆盖无 Team、已有 Team、相同 Key 重放、普通/首 Team 跨入口同 Key 冲突、新 Key 冲突、并发单基础与 Owner/Workspace/5 Role/Grant/Personal Agent/Event/Outbox/Receipt 完整性；证据见 [M7-A04](../testing/M7-A04-当前账号Onboarding与首Team事务.md) |
| `M7-A05` | FEATURE（已完成） | D05,D06,I06,A02 | application/server | 已实现邀请创建、Keyset 列表、隐私化 Preview、撤销与已登录账号 Accept；`MEMBER_MANAGE` 裁决 Owner/Admin，创建重放只返回 Receipt，Accept 与撤销统一 Invitation→Team 锁序；新账号带邀请注册继续复用 `TeamInvitationAcceptanceService` | 应用/API/安全/装配与 PostgreSQL 专项覆盖 Admin/Member、Token 首次返回、Preview 隐私、Membership/Role Grant、停用目标 Role 在 Membership 写入前失败关闭、稳定错误、Digest/ID 同行锁和 Token 不落库；证据见 [M7-A05](../testing/M7-A05-Team邀请管理与当前账号接受.md) |
| `M7-A06` | TASK（已完成） | A01..A05,I05 | server | 已统一公开 SPA/认证 API/业务 API/Prometheus 路由、401/403/CSRF/Origin/413 安全信封、`no-store`、认证请求预算和浏览器安全响应头；业务 API 对 JSON/HTML Accept 均不触发 Basic 弹窗，Origin 只比较 Spring 代理规范化后的请求 URI | 核心路由/账号场景覆盖匿名、已登录、锁定、禁用、CSRF 缺失、跨源/代理同源、伪造原始 Forwarded Header 失败、声明/实际请求预算、HTML/API Accept、安全响应头和机器 Challenge；证据见 [M7-A06](../testing/M7-A06-认证路由与浏览器安全边界.md) |
| `M7-A07` | TASK（已完成） | A01..A06,D06 | application/server/docs | 已冻结 8 个 Auth/Account/Onboarding/Invitation 请求 DTO、稳定错误矩阵、单值幂等/强版本、重放 Header、10 个 V1 Audit 映射及 Jackson 2/3 与 Spring Bean 唯一装配；身份持久化过载按路由折叠为稳定 503 | [M7-A07](../testing/M7-A07-开放用户API与装配合同.md)；12 个专项场景覆盖未知字段失败、服务端坐标/响应敏感字段扫描、重复 Header、错误兼容、有界执行器过载、Audit Registry、文档合同、条件 Controller 和双 Jackson Mapper |

## 9. 前端

| ID | 类型 | 依赖 | 涉及模块 | 实施内容 | 验证 |
|---|---|---|---|---|---|
| `M7-F01` | TASK（已完成） | S04 | web | 已建立 AuthLayout、品牌区、认证卡片、原生语义表单控件、12–128 字符密码预算提示、错误摘要和命名空间公开页面 Token；复用 CrewScope Logo，密码显隐只存在组件内存 | [M7-F01 公开身份前端基础](../testing/M7-F01-公开身份前端基础.md)；25 个新 Variant 覆盖 11 个认证状态的桌面/390px、Reduced Motion 和深浅背景，浏览器实测焦点、单一 main、零正 tabindex 与无窄屏溢出，全量 Vitest 461/461、Histoire 18 Story/141 Variant |
| `M7-F02` | FEATURE（已完成） | F01,A02,A06 | web | 已实现正式 `/login`、闭合 Identity Gateway、匿名 Session/内存 CSRF 准备、标识/密码提交、统一 Credential 错误、限流/离线/超时、重复提交阻断、安全原目标恢复和已登录重定向；密码不进入 URL、Store 或日志 | [M7-F02 正式登录页与安全返回目标](../testing/M7-F02-正式登录页与安全返回目标.md)；定向 Vitest 44/44、全量 489/489、Playwright Desktop/390px 6/6、Axe 零违规、Build/Histoire 通过，敏感字段门禁覆盖 53 个生产文件/18 个 Story |
| `M7-F03` | FEATURE（已完成） | F01,A01,A06 | web | 已实现正式 `/register`、Registration Mode、邀请 Fragment 内存消费、注册字段/密码双预算、闭合 Gateway、内存幂等恢复和登录页入口；普通注册进入 Onboarding，邀请注册原子入 Team 后跳过 Onboarding | [M7-F03 正式注册页与邀请注册上下文](../testing/M7-F03-正式注册页与邀请注册上下文.md)；Vitest 92 文件/509 测试、Playwright Desktop/390px 6/6、Axe 零违规、Build/Histoire 通过，敏感字段门禁覆盖 55 个生产文件/18 个 Story |
| `M7-F04` | FEATURE（已完成） | A02,F02,F03 | web | 已实现真实 AuthStore、启动 Session 单飞恢复、内存 CSRF、Router Guard、`authentication_required` 401 统一恢复、跨标签退出、全领域 Store 身份隔离与迟到响应阻断；已删除生产 `bootstrapPrincipal` 注入 | [M7-F04 AuthStore 与会话路由守卫](../testing/M7-F04-AuthStore与会话路由守卫.md)；Vitest 94 文件/521 测试、Playwright 208/208、Build/Histoire 18 Story/141 Variant、57 个生产文件/18 个 Story 敏感字段门禁通过 |
| `M7-F05` | FEATURE（已完成） | A04,F04 | web | 已实现正式 `/onboarding`、Team/Workspace/Owner/Personal Agent 初始化说明、首 Team 闭合命令、内存 CSRF/幂等恢复、Session/Team/Agent 逐层复验、已有 Team 跳过和完成后 Conversation 入口；不伪造百分比进度 | [M7-F05 首次 Team Onboarding](../testing/M7-F05-首次Team-Onboarding.md)；Vitest 98 文件/537 测试、Playwright 214/214、Build/Histoire 19 Story/145 Variant、58 个生产文件/19 个 Story 敏感字段门禁通过，真实浏览器覆盖桌面/390px、焦点、恢复态和零溢出 |
| `M7-F06` | FEATURE（已完成） | A03,F04 | web | 已实现正式 `/account`、闭合 Account Gateway/Store 与 AppShell 用户菜单；资料修改按字段差异提交，用户名/邮箱变更要求当前密码 Step-up，改密与全部设备退出后要求重新登录，当前设备退出只撤销本 Session | [M7-F06 账号设置与会话安全](../testing/M7-F06-账号设置与会话安全.md)；Vitest 102 文件/556 测试、Playwright 226/226、Build/Histoire 20 Story/148 Variant、65 个生产文件/20 个 Story 敏感字段门禁通过，覆盖强 ETag、冲突权威重载、离线保护、确认框焦点、Unicode 头像回退和密码零持久化 |
| `M7-F07` | FEATURE（已完成） | A05,F03,F04 | web | 已在 Team 成员页实现邀请创建/Keyset 列表/复制/撤销，并交付正式 `/invite#token=...` Preview/Accept；匿名已有账号通过内存证明登录后返回 Accept，新用户由正式注册事务原子入 Team | [M7-F07 Team 邀请管理与邀请接受体验](../testing/M7-F07-Team邀请管理与邀请接受体验.md)；Vitest 107 文件/577 测试、Playwright 240/240、Build/Histoire 21 Story/153 Variant、73 个生产文件/21 个 Story 敏感字段门禁通过，覆盖一次性链接、过期/已接受、邮箱不匹配、无管理权限、双视口、Fragment 及时清理和焦点恢复 |
| `M7-F08` | HARDENING（已完成） | F01..F07,I08 | web/docs | 已收口认证与 Onboarding 全状态、响应式、Axe、视觉、Story、公开字段和部署入口；Coverage 纳入四个 M7 身份领域和五个正式页面，生产 Web 门禁拒绝业务 Basic Auth 文案与占位身份 | [M7-F08 认证与 Onboarding 前端收口](../testing/M7-F08-认证与Onboarding前端收口.md)；Vitest 111 文件/625 测试及扩展 Coverage、Histoire 21/153、双视口 Playwright/Axe 240/240、生产构建、README/Demo 和 78 个生产文件/21 个 Story 敏感字段 CI 门禁通过 |

## 10. 质量、安全与发布

| ID | 类型 | 依赖 | 涉及模块 | 实施内容 | 验证 |
|---|---|---|---|---|---|
| `M7-Q01` | HARDENING（已完成） | D01..D06,I02..I05,A01..A07,F02..F04 | all/security | 已建立本地认证固定攻击集和 Docker 强制一键门禁，覆盖密码、账号枚举、暴力尝试、Session 固定/劫持、CSRF、Cookie、Origin、开放重定向、日志与响应泄漏 | [M7-Q01 本地认证安全硬化与固定攻击集](../testing/M7-Q01-本地认证安全硬化与固定攻击集.md)；128/128 固定攻击阻断，Java 194/194、Web 61/61，零失败、零错误、零跳过 |
| `M7-Q02` | HARDENING（已完成） | D07,D08,I01..I07,A01..A05 | domain/application/infrastructure | 已完成账号、绑定、邀请和迁移的固定并发/故障矩阵，验证 PostgreSQL、Redis 与进程故障下无重复账号、Membership 或未审计状态；修复邀请注册双事件复用命令幂等键导致事务冲突的问题 | [M7-Q02 身份邀请并发故障与事务收敛](../testing/M7-Q02-身份邀请并发故障与事务收敛.md)；72/72 固定故障样本和 Java 140/140 通过，覆盖八路注册/邀请竞争、V1→V32 与 V30→V32、Redis、事务四窗口、Operator 升级及提交响应丢失恢复，零失败、零错误、零跳过 |
| `M7-Q03` | HARDENING（已完成） | F01..F08,Q01,Q02 | server/web/e2e | 已交付完整双用户生产 E2E：注册 A、创建 Team、邀请 B、B 注册并接受、各自 Personal Agent/权限、TEAM Conversation 协作、退出和重新登录 | [M7-Q03 双用户真实协作与会话恢复](../testing/M7-Q03-双用户真实协作与会话恢复.md)；真实 PostgreSQL/Redis、V1→V32、Spring Boot 与生产 Web，Desktop/Narrow 各使用两个独立 BrowserContext，刷新、Session 过期、API 重启、Audit 双主体和分别重登/退出 `2 / 2 passed` |
| `M7-Q04` | HARDENING（已完成） | 全部 M7 | all/ci/deploy | 已聚合 M7 Release Gate，验证升级部署、Bootstrap 弹窗消失、OPEN/INVITE_ONLY/DISABLED 三 Profile、备份恢复和 CI 安全门禁 | [M7-Q04 Release Gate](../testing/M7-Q04-Release-Gate.md)；Maven 3056/3056、Vitest 652/652、Histoire 21/153、Profile E2E 1/1、343 个文档、生产依赖审计及 V26..V32 恢复合同通过；Linux amd64 Server RC 原生构建、V30→V32、Operator 登录、重启 Session 与 Secure Cookie 合同通过 |

## 11. 配置与 API 基线

### 11.1 关键配置

```yaml
crewscope:
  security:
    mode: local
    password:
      hash-permits: 2
    session:
      enabled: true
      ttl: 12h
      maximum-sessions: 5
      cookie:
        secure: true
        same-site: Lax
  registration:
    mode: OPEN
  invitation:
    ttl: 7d

spring:
  session:
    # Spring Boot 4 使用 data.redis 前缀，M7-I02 以配置测试防止静默回退。
    data:
      redis:
        namespace: crewscope:session
```

生产或公网部署必须使用 HTTPS 并启用 Secure Cookie。开发环境可显式关闭 Secure，但配置 Guard 必须拒绝把该设置用于生产 Profile。密码哈希使用 M7-S03 实测确定的 Argon2id M32/T3/P1；2C2G/8C16G 分别使用 2/4 个有界 Hash Permit，完整参数和重测条件由 [ADR-025](../adr/ADR-025-本地密码与登录防护参数.md) 管理。

### 11.2 公开 API

```text
POST   /api/v1/auth/register
POST   /api/v1/auth/login
POST   /api/v1/auth/logout
GET    /api/v1/auth/session

GET    /api/v1/account
PATCH  /api/v1/account
POST   /api/v1/account/password
POST   /api/v1/account/sessions/revoke

GET    /api/v1/onboarding
POST   /api/v1/onboarding/team

POST   /api/v1/organizations/{organizationId}/teams/{teamId}/invitations
GET    /api/v1/organizations/{organizationId}/teams/{teamId}/invitations
POST   /api/v1/organizations/{organizationId}/teams/{teamId}/invitations/{invitationId}/revoke
POST   /api/v1/invitations/preview
POST   /api/v1/invitations/accept
```

登录和注册接受 JSON，请求体字段采用固定白名单。Session Cookie 不进入响应 DTO。邀请 Token 只允许出现在创建成功的一次性响应、邀请链接 Fragment、Preview/Accept POST Body 和页面进程内短期状态，不进入 HTTP Path/Query、查询列表、Telemetry、错误详情或持久浏览器存储。

## 12. M7 Release Gate

M7 完成需要同时满足：

1. 浏览器访问 Web 不再出现 HTTP Basic 原生认证弹窗；
2. OPEN、INVITE_ONLY 和 DISABLED 注册模式行为与配置一致；
3. 两个用户拥有独立 Account、Session、Principal、TeamMember、Personal Agent 与 Audit 身份；
4. 注册、登录、退出、Session 过期、密码修改和全部 Session 撤销闭环通过；
5. 首次 Team 创建只复用一条 M1 初始化事务，不产生重复 Workspace、Role Grant 或 Personal Agent；
6. 邀请 Token 明文不落库且只能成功接受一次；
7. 密码明文/哈希、Session ID、认证 Cookie 和邀请 Token 不进入日志、事件、Audit、Trace、指标或公开 DTO；防 CSRF Token 只出现在受控 CSRF Cookie、Session 公开投影和同源写请求 Header，不进入其他浏览器持久存储或 Telemetry；
8. Session 固定、CSRF、开放重定向、账号枚举、暴力尝试和跨 Organization/Team 攻击全部阻断，一个 Team 的管理权限不会进入另一个 Team 的浏览器 Scope；
9. Redis、API 进程和数据库故障不会产生半注册、重复 Membership 或已退出 Session 恢复；
10. Team Beta 从 V30 升级到 V32 后 Operator 可通过登录页进入，既有 M0–M6 数据和权限保持有效；
11. 登录、注册、Onboarding、账号和邀请页面通过双视口、键盘、Axe、视觉和离线/错误状态门禁；
12. 全量 Maven、Vitest、生产构建、Histoire、Playwright、镜像、迁移、备份恢复和文档检查通过。

## 13. 建议执行波次

| 波次 | 任务 | 可演示结果 |
|---|---|---|
| W1 认证决策 | S01–S04 | JSON 登录、Session/CSRF 与完整页面原型完成技术验证 |
| W2 账号内核 | D01–D08 | Account、Identity、Credential、Binding、Invitation 与 V31/V32 契约稳定 |
| W3 登录纵切 | I01–I05、A01–A03、F01–F04 | 用户可注册、登录、恢复 Session、修改账号并退出，原生弹窗消失 |
| W4 协作纵切 | I06–I08、A04–A07、F05–F08 | 首次建 Team、默认 Personal Agent、邀请协作和部署升级闭环 |
| W5 发布门禁 | Q01–Q04 | 固定攻击/故障、双用户 E2E、V30→V32 与 M7 Release Gate 通过 |

M7 共拆分为 4 个 Spike、8 个领域/迁移任务、8 个基础设施任务、7 个应用/API 任务、8 个前端任务和 4 个质量任务，共 39 项，现已全部完成。公开身份基础、正式登录/注册、全局身份恢复、首次 Team Onboarding、账号安全工作区与 Team 邀请纵切已交付隔离于 AppShell 的 Auth Token/组件、闭合 Identity/Onboarding/Account/Invitation Gateway、真实 AuthStore、Session/内存 CSRF、Router Guard、401 与跨标签退出恢复、全领域 Store 清理、密码双预算、强 ETag、当前/全部设备退出、邀请创建/列表/撤销、Fragment 内存证明、登录/注册衔接、Session/Team Scope 接受复验和敏感字段零持久化。F08 已扩展真实 Coverage 分母、补齐公开错误状态矩阵，并统一收口 Histoire、双视口 Playwright/Axe、视觉、README/Demo 和 CI 身份敏感字段门禁；Q01 已冻结并阻断 128 个认证攻击样本；Q02 已通过 72/72 固定并发故障样本和 Java 140/140 门禁；Q03 已在真实生产栈以两个独立 BrowserContext 完成双用户邀请、Personal Agent、Conversation、重启、审计、过期和会话恢复，Desktop/Narrow `2 / 2 passed`；Q04 已通过 Maven 3056/3056、Vitest 652/652、三 Profile 真实 E2E、部署恢复、文档与生产依赖门禁，并在 Linux amd64 Server RC 完成原生镜像构建、V30→V32、Operator 登录、API 重启 Session 与 Secure Cookie 验证。提交前 Review 进一步将 Session 顶层能力收敛为账号级权限，AuthStore 按当前 Team 切换权限，避免跨 Team UI 权限并集；同时修复 Q04 独立构建开关的前置检查，M7 Release Gate 本机与服务器结论均为 `PASS`。
