# M9-F02：AppShell 与信息架构验证

## 交付内容

- 侧栏按“工作 / 团队 / 配置”三组展示，并根据既有 `can()` 权限过滤；移除 WorkGraph、治理与设置等不可用占位按钮。
- 导航文案统一为中文，保留 Agent、GitHub、Inbox 等产品术语；路由名称与深链接保持不变。
- 侧栏折叠状态通过 `cs.pref.device.rail-collapsed.v1` 版本化偏好持久化，导航区独立滚动，账号区保持固定。
- 顶栏搜索入口跳转工作项工作台，通知入口跳转 Inbox；模式切换与侧栏导航补齐 `aria-current="page"`。
- 主题切换沿用版本化主题偏好，面包屑由 AppShell 统一挂载。

## 验证结果

- 前端单元测试、构建与质量门禁通过。
- AppShell 侧栏固定账号区、独立滚动和权限过滤回归通过。
- AppShell Desktop/Narrow 视觉基线已按预期导航分组变化重拍。
- 既有路由名、查询参数和权限判定未改变；未新增领域 API、事件或数据库迁移。

