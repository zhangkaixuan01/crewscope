# M9-A06 配置查询与配置健康验证

## 已交付

- Agent 配置服务新增按 Revision 的只读读取，复用既有可见性与 Team 权限边界。
- 新增全量公开载荷接口，使用配置 hash ETag；历史 Revision 保持不可变，没有回滚或应用历史版本写路径。
- 新增配置健康派生服务与 `/configuration-health` 查询，统一从 Agent 配置、模型连接、CredentialStore 和 Provider Connection 读取事实。
- 健康响应仅返回稳定状态、reason、责任方和 action，不返回凭证明文、endpoint、宿主路径或外部原始错误。

## 验证命令

```bash
./mvnw -q -pl crewscope-application,crewscope-server -am -DskipTests compile
./mvnw -q -pl crewscope-server -am \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -Dtest=AgentConfigurationControllerM5A03Test,TeamSetupReadinessControllerM8A01Test test
node scripts/generate-openapi-types.mjs
node scripts/check-openapi-drift.mjs
git diff --check
```

随后 M9-A07 增加主体目录端点后，OpenAPI 操作数由 208 增加到 212；M9-A02 的统一搜索端点使当前基线为 213。A06 本身新增三个只读端点，状态机仍为 16 个聚合。
