# M9-Q01：质量门禁与基线

`scripts/check-m9-q01.mjs` 是 M9 的回归门禁。它扫描生产前端代码中最容易造成体验回退的三类问题：原生弹窗、要求用户粘贴 UUID/Principal ID 的输入，以及裸 `title` 属性。

门禁采用可审计基线：当前基数写入 `docs/quality/M9-门禁基线.json`，新增违规会让 CI 失败；每次偿还存量问题时同步下调基线。视觉快照仍由 Playwright 管理，功能可达性由 E2E 独立验证。

本地运行：

```bash
node scripts/check-m9-q01.mjs
```
