# M9-F06 暗色主题与双密度验证

## 交付范围

M9-F06 为 CrewScope 提供设备级主题与密度偏好：主题支持跟随系统、浅色、深色，密度支持舒适、紧凑。偏好使用带版本号的 `usePreference` 存储在浏览器 `localStorage`，不进入服务端领域状态。

首帧由 `crewscope-web/index.html` 在 Vue 挂载前读取并校验偏好，提前写入 `data-theme` 与 `data-density`，避免刷新时先显示浅色再切换。运行时由 `App.vue` 统一应用，系统主题变化通过 `matchMedia` 同步；顶栏按钮和账号设置页共享同一组键与校验规则。

## 实现要点

- `usePreference` 支持可选运行时校验器，版本匹配但值非法时回退默认值；跨标签页同步同样经过校验。
- 语义 Token 在 `[data-theme="dark"]` 下覆盖画布、表面、边框、文字、反馈、导航选中态、Diff 增删/上下文色和代码语法色。
- 认证页通过 `.cs-auth-theme` 的暗色覆盖层适配未登录入口；不会把认证卡片恢复成浅色硬编码。
- 密度只调整控件高度和面板间距，不改变字号阶梯；移动端隐藏顶栏密度按钮，账号设置仍可调整。
- Diff 增删同时保留 `+`/`-` 前缀与色彩编码，暗色下使用独立对比度校准的 Token，满足色觉差异用户的辨识需求。

## 验证矩阵

| 维度 | 组合 | 结果 |
|---|---|---|
| 主题 | system + 系统浅色/深色、light、dark | 通过：首帧与运行时解析一致 |
| 密度 | comfortable、compact | 通过：控件高度/面板间距切换，字号不变 |
| 数据恢复 | 缺失、版本不匹配、JSON 损坏、非法枚举、跨标签页事件 | 通过：安全回退或忽略非法事件 |
| 认证入口 | 登录、注册、Session 错误页 | 通过：暗色语义 Token 生效 |
| 执行阅读 | Diff 文件树、Patch、增删行、语法片段、降级提示 | 通过：暗色语义色与前缀双编码 |
| 动效 | `prefers-reduced-motion: reduce` | 通过：既有全局与认证页降级规则保留 |
| 响应式 | Desktop、1100px、767px、390px | 通过：导航折叠/抽屉与操作区不溢出 |

## 自动化结果

在 `crewscope-web` 目录执行：

```text
pnpm build                 PASS
pnpm test -- --run         134 files / 719 tests PASS
pnpm check:quality         PASS
pnpm check:sensitive       PASS
node scripts/check-doc-links.mjs  PASS (391 Markdown files)
git diff --check            PASS
pnpm exec playwright test e2e/m9-theme-density.spec.ts  PASS (6 tests: Desktop + 390px)
```

F06 不改变服务端接口、权限、事件或数据库迁移；M0–M8 既有测试继续作为回归基线。
