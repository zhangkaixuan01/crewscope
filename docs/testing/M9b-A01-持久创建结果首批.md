# M9b-A01：持久创建结果首批

> 日期：2026-09-22<br>
> 状态：历史首批记录；后续前后端交付与父包状态以[完整验证记录](M9b-A01-创建编号恢复与基本编辑完成.md)为准<br>
> 正式协议：[创建结果与命令恢复契约](../api/M9b-创建结果与命令恢复契约.md)

## 1. 切片卡

| 字段 | 本批约定 |
| --- | --- |
| 标识/归属 | M9b-A01/durable-create-result；属于主计划“创建结果定位与读侧等待”的后端部分；R02/R05/R06 的基础能力 |
| 用户结果 | 为项目、工作项、对话创建丢响应后的准确定位提供耐久坐标；本批不声称用户界面已经能够刷新恢复 |
| 输入与前置 | S01 D01、F01 已完成；既有创建权限/Scope/原键，查询需原 actor 且当前可见 |
| 变更边界 | command 应用 Port/Query、JDBC、V40、三类创建处理器、Controller/配置、OpenAPI；不增加服务，不改部署/原创建回执/编号/执行 |
| 状态与失败 | 200 原事实；404 统一无可披露结果；401 认证失效；异常传播且写入回滚；不根据空结果自动重建命令 |
| 接口与交接 | 正式 GET + 原 Idempotency-Key Header；下一批接 F01 意图层和三类创建页面，尚无 UI/路由/本地恢复存储变更 |
| 验证与证据 | C02/C16 后端部分：应用授权、三类创建重放、HTTP Header/错误/no-store、真实 PostgreSQL 并发/回滚/升级；布局 L 项本批不涉及，但父包仍须完成 |
| 兼容与恢复 | 旧四字段回执不变；旧记录不猜测回填；无 TTL；V39 前滚 V40，不操作现有部署数据库 |

## 2. 已实现

- 原 actor + 当前可见性的受权结果查询；跨组织、非本人、管理员代查、失去成员/对象访问权均不披露结果。
- 项目/工作项/对话创建在原事务内追加不可变坐标，结果失败使原事务失败；重放不重复创建或追加结果。
- V40 复合回执身份外键、Scope 外键、类型坐标检查、版本检查和 UPDATE/DELETE 禁止；旧数据保持原样。
- 成功与错误结果均 no-store；单值 Header 复用原校验；基础设施错误不会被当成“无结果”。
- Controller 生成目录由 217 增至 218 操作，并同步漂移基线。

## 3. 自动验证

本批只使用 mock/WebTestClient 与**专属短命 PostgreSQL Testcontainer**，未启动停止中的产品服务或 Compose，也未使用真实 Provider 凭据、真实部署数据库。没有真人验收步骤。

```bash
./mvnw -q -pl crewscope-server -am \
  -Dtest=CommandResultQueryServiceTest,CommandResultControllerTest,JdbcCommandResultIntegrationTest,WorkItemCommandServiceTest,WorkProjectApplicationServiceTest,ConversationApplicationServiceTest \
  -Dsurefire.failIfNoSpecifiedTests=false test
./mvnw -q -pl crewscope-application -am test
./mvnw -q -pl crewscope-server -am \
  -Dtest=WorkProjectControllerTest,WorkItemControllerTest,ConversationControllerTest \
  -Dsurefire.failIfNoSpecifiedTests=false test
node scripts/generate-openapi-types.mjs
node scripts/check-openapi-drift.mjs
node scripts/check-doc-links.mjs
```

环境使用本机 JDK 17；下表为最终复跑结果。首轮测试编译时误用了不存在的时间工厂，已改为固定时间；首轮并发夹具让两次请求共用内部 commandId，与真实每次调用新建内部 ID 不符，已修正为同原键/hash、不同内部 ID 后通过。未放宽 SQL 约束或跳过失败测试。

| 测试层 | 断言 | 本批结果 |
| --- | --- | --- |
| CommandResultQueryServiceTest | 原 actor、三类当前权限、撤权/隐藏/删除、组织/账号状态隔离、异常不伪装 404 | 8 项通过 |
| 三类创建应用测试 | 创建坐标与原回执相同；原键重放一份事实/结果 | 工作项 6、项目 7、对话 13 项通过 |
| CommandResultControllerTest | 正式路径、最小 DTO、缺失/重复/非法 Header、401/404/no-store | 9 项通过 |
| JdbcCommandResultIntegrationTest | 7 项真实 PG：新 adapter/连接可读、并发原键、两类全事务回滚、V39 升级无回填、不可变/错配、MANDATORY/未完成回执拒绝 | 7 项通过，0 skipped；实际 PostgreSQL 17.10 |
| domain/application 全量 | 接口新增方法后的既有业务回归 | 领域 97 文件/696 项，应用 158 文件/690 项，均无失败/跳过（包含上述应用定向项，不重复累加） |
| 生成物/文档 | 218 操作漂移门禁、相对链接 | 通过；413 份 Markdown 链接检查、git diff --check 通过 |
| Web 类型 | 生成目录更新后 pnpm lint | 通过；本批无 UI 交互改动，不把类型检查记为浏览器恢复验收 |

原项目/工作项/对话 Controller 另回归 5/5/6 项通过，原创建四字段响应保持兼容。各 Java 测试详情位于对应模块的 target/surefire-reports；测试产物不提交仓库。

## 4. 后续执行顺序与不关闭项

以下是首批交接时的历史待办，现均已在[完整交付](M9b-A01-创建编号恢复与基本编辑完成.md)中完成；保留本节用于区分当时的证据范围。

1. 在本接口上接三类创建的准确定位、读侧等待、原意图恢复，补丢响应/同名/翻页/切 Scope 的真实浏览器证据。
2. 普通工作创建与 TaskIntent 统一项目锁和自动编号；项目自动短代号，兼容显式旧 Key/hash/旧链接，再移除普通创建 Key 输入。
3. 基本字段编辑：字段缺省/null、归档只读、强版本/权限/幂等/事件，接详情编辑/冲突和列表/Today 同步，保证历史执行快照不变。
4. 汇总 C02 等全部 A01 验收后才能关闭父包。本批不宣称跨刷新恢复、自动编号、编辑或所有创建类命令均已完成。
