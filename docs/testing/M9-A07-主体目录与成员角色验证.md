# M9-A07 主体目录与成员角色验证

已交付：

- 新增 Team 作用域主体目录查询，统一列出可见 USER 与 AGENT，并支持显示名前缀搜索与有界分页。
- 成员列表从既有角色授予事实派生 `roles`，不新增角色表或角色写路径。
- 主体目录公开字段白名单不含邮箱、外部身份、凭证和资源标题。

验证命令：

```bash
./mvnw -q -pl crewscope-application,crewscope-server -am -DskipTests compile
./mvnw -q -pl crewscope-application,crewscope-server -am \
  -Dtest=TeamApplicationServiceTest,TeamControllerTest,PrincipalDirectoryControllerTest \
  -Dsurefire.failIfNoSpecifiedTests=false test
node scripts/generate-openapi-types.mjs
node scripts/check-openapi-drift.mjs
node scripts/check-doc-links.mjs
git diff --check
```

`PrincipalDirectoryControllerTest` 覆盖 USER/AGENT 返回、角色字段、`nextOffset`、`Cache-Control: no-store`、敏感字段白名单和超过 200 条的分页参数拒绝；应用层与 Team 控制器回归测试保持通过。
