# M9b-A04：项目构建配置与支持矩阵完成

日期：2026-09-23

## 已完成

- 新增版本化 `ProjectExecutionDefaults` 领域对象、JDBC repository 与 Flyway V41；无配置以 version 0 表示，完整 PUT 替换，清除可选值使用 null。
- 新增项目默认 GET/PUT/options API，复用 RepositoryBinding Owner/Admin 权限、强 If-Match、幂等键、仓库 ACTIVE/version/hash 校验和逐字段 source/availability/reason 投影。
- 保留 Java BuildProfile v1 canonical hash/reconstitute；新增 Node schemaVersion=2，固定 Node 24.19.0/npm 11.17.0、NPM PREPARE/COMPILE/TEST 命令和独立 profile hash。
- Coding runner 识别 NPM 构建工具并拒绝模块/测试 selector；前端 Gateway、Store 与 Repository Settings 的“项目默认执行配置”面板已接入，不要求填写内部 Key 或宿主命令。

## 验证

- Java：Domain `BuildProfileTest`/`ProjectExecutionDefaultsTest` 14/14；Server 及其依赖模块编译通过；`BootstrapOperatorProvisioningM7I07IntegrationTest` 9/9 通过，确认 V30 → V41 前滚（含 V41）。此前全量 Infrastructure reactor 在 Testcontainers 资源回收阶段出现连接拒绝，已定位为环境级长跑失败，不把该次结果记为全量 PASS。
- Web：`pnpm build`、`pnpm lint`、`pnpm lint:eslint`、`pnpm lint:style`、`pnpm check:sensitive`、`pnpm check:enum-labels`、`pnpm check:q01` 全部通过；`RepositorySettingsPage.spec.ts` 10/10 通过，覆盖默认配置入口且不要求用户填写内部 Key。
- 契约/文档：OpenAPI、状态机、Agent limit 生成物重新生成并通过 drift（223 operations、16 aggregates）；`node scripts/check-doc-links.mjs` 与 `git diff --check` 通过。

## 支持边界

Node v2 当前是受控目录和 runner 接线，真实 Node 24/npm 11 样本与每架构镜像 digest 仍需 Q02 授权环境补证；不把本机缓存实验镜像当成发行支持。pnpm/yarn、多 package、任意 shell、未知栈继续明确拒绝。
