# M6-A01 至 M6-A07 提交前审查

> 审查日期：2026-08-27<br>
> 范围：Activity、Inbox、Audit、Lark/Notification 管理、Team Observer、Operations 和 Correlation API<br>
> 结论：已完成收敛与回归，可提交

## 1. 审查结论

M6-A01 至 M6-A07 的主体分层、构造器注入、公开 DTO 白名单、稳定 Keyset、强 ETag、幂等 Receipt、安全 SSE 和 PostgreSQL 查询预算符合 M6 冻结契约。Correlation 查询保持固定两查询上限，Task Timeline 在分页前过滤未审查 EventType。

提交前需要收敛三类边界：

1. Inbox、Lark Mapping 和 Notification Delivery 翻页必须先验证当前 Team 或 `PROVIDER_MANAGE` 权限，再解码 Cursor；
2. Lark ProviderBinding 与 Notification Delivery 版本冲突必须返回稳定 `409 optimistic_lock_conflict`，不能落入 `500 internal_error`；
3. Team Observer 已建立的 SSE 要在每个业务帧披露前复验成员和 Invocation 归属；
4. Projection Definition、Pointer、Generation 和 RebuildJob 版本漂移要统一返回 `409 optimistic_lock_conflict`，不得落入 500。

## 2. 实施与验收

- 应用层提供只执行当前授权的预检入口，HTTP 层不自行重建权限规则；
- Cursor 预检测试使用失效成员/管理员与非法 Token，确认 Codec 不会被调用；
- 版本冲突测试校验 HTTP 409、`currentVersion`、`expectedVersion/actualVersion` 和无外部写入；
- Team Observer 测试在首帧后撤回成员权限，确认终态摘要帧不会继续披露；
- M6-A01 至 M6-A07 专项回归 84 项全部通过；
- `./mvnw clean verify` 六模块共 2343 项测试全部通过，0 failure、0 error；
- `node scripts/check-doc-links.mjs` 通过 287 份 Markdown 链接检查；
- `git diff --check` 通过。
