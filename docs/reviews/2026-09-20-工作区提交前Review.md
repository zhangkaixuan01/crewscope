# 2026-09-20 工作区提交前 Review

范围：`aa82f11` 之后本次提交的全部既有未提交改动及新增文件，包括部署/后端、初始化与备份脚本、测试合同、README/运维文档、M9b 计划与三份产品 Review、M10–M12 调整和历史文档注记。不是重新实施 M9b，也不是全仓逐行安全认证。

## 本轮修正

| 问题 | 修正及验证 |
| --- | --- |
| Nginx 只覆盖 X-Forwarded-For，标准 Forwarded 仍可优先影响 Spring 请求地址与来源 | 清除 Forwarded、冲突端口/前缀/SSL 头，保留受控 Host/XFF 与可选上游 HTTPS；部署合同锁定规则 |
| 运行/执行目录仅按原始字符串拒绝根目录，`.`、`..`、符号链接可绕过 | 增加共享物理路径校验，在 mkdir/chmod/chown 前拒绝系统根、主目录/仓库/运行目录祖先；拒绝 env 和执行子目录符号链接；临时目录反例回归 |
| 并发首次初始化使用 mv，可能覆盖另一进程已发布的密钥 | 完整临时文件以不可覆盖的链接发布；发生竞争明确要求重试，原密钥不替换 |
| 冷备份先复制 env、再由 down 初始化补密钥，备份可能缺新密钥 | 复制前完成兼容初始化；校验备份位置的实际路径和恢复 env/执行路径一致性；补隔离编排测试 |
| M7 浏览器门禁复用本地镜像，可能验证旧代码 | Q03/Q04 在启动前显式 build 当前源码；日常 quickstart up 仍复用镜像，不增加普通启动负担 |
| 部署默认值测试受操作者环境变量影响；简化时删掉无关配置断言 | 合同测试不继承 CrewScope/Compose 覆盖；恢复开发数据库环回地址和 GitHub 配置层级保护 |
| M10 切片表仍以 M9-Q02 为入口，M11 附录仍规划重复成员命令/Controller | 统一 M9b-Q02 前置；M11 只消费 M9b 生命周期事实；同屏验收明确用隔离测试账号，不设真人测评 |
| Runbook 残留自定义构建页面能力暗示及跨路径恢复歧义 | 同步当前 Maven/Java 17 与已知业务限制；明确同路径空目标恢复、失败残留、停机状态、未加密/可信备份和 shell 覆盖不入备份 |

部署的四服务、源码构建、HTTP、自动 env 密钥及 API 合并 Worker 保持不变。不恢复强制 TLS、外部 Secrets、独立 Worker、观测栈或镜像发行要求。业务鉴权、人审和凭据隔离仍保留。

## 本轮实际验证

- 前端 `pnpm test`：160 个文件、924 项通过。
- 前端 `pnpm build`、`pnpm check:quality`：类型、ESLint、Stylelint、格式与生产构建通过；`check-web-bundle-budget.mjs` 通过。
- 定向 Maven：7 个测试类、32 项通过，零失败/错误/跳过；包括 HSTS、部署 Guard、组合 Worker、导入配置、登录防护、WorkDesk 路由，以及新增的实际 Bean 条件与两个 JDBC Adapter 的 CGLIB 可代理性测试。
- `./scripts/m9-q02-local-gate.sh contracts-only` 通过：包含 OpenAPI/状态机生成漂移、部署、旧恢复协议、安全/体验、依赖和文档合同。
- 新 `check-team-beta-snapshot.mjs` 使用临时目录与 Docker 替身验证完整密钥/卷清单、先停后归档、校验损坏/非空目标/符号链接路径拒绝及恢复后保持停止；它不证明真实归档数据可恢复。
- Shell 语法与 `git diff --check` 通过；M9b 的 15 个父包/主责、依赖无环及图表传递前置一致、R01–R43、C01–C16、D01–D08 只读校验通过。

定向后端命令（仓库根目录）：

```bash
./mvnw --batch-mode --no-transfer-progress -pl crewscope-server -am \
  '-Dtest=SecurityConfigurationTest,TeamBetaSimpleDeploymentConfigurationTest,TeamBetaDeploymentGuardM6I09Test,TaskWorkerConfigurationM3I09Test,GitHubRepositoryImportApplicationConfigurationM8Q02Test,LoginDefenseBoundaryM7I04Test,JdbcWorkDeskExecutionRouteTest' \
  -Dsurefire.failIfNoSpecifiedTests=false test
```

## 未执行与剩余边界

本轮未恢复已停应用、未启动真实 Compose 栈、未跑真实冷恢复/浏览器端到端、全量 Java 或原生 Linux 新拓扑验证，也未调用模型、发飞书、创建 PR 或发布镜像。2026-09-19 的运行记录保持其原版本含义，不作为本轮新增修正的运行通过证据。

M9b 的 R01–R43 仍是待实施产品问题，尤其远端 HTTP 提交兼容、自动编号与 GitHub 首用断链；本次提交计划不等于修复这些功能。默认 HTTP 不加密传输，API 持有宿主 Docker 管理权限，适用于自有/专用主机；限制已在 README/Runbook 明示。
