package io.crewscope.agentscope;

/** Lifecycle outcome represented by a safe platform audit record. */
public enum AgentExecutionAuditOutcome {
    STARTED,
    COMPLETED,
    FAILED,
    CANCELED
}
