package io.crewscope.agentscope.coding;

/** User control that terminates the current finite AgentScope call at its next safe boundary. */
public enum CodingSpecialistControlAction {
    PAUSE,
    CANCEL
}
