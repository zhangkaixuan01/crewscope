# Java Spring Lab

这是 CrewScope M4 Coding Evaluation v1 的 Agent 可见仓库 Fixture。每个评测任务只授权一个目标文件；验收测试由平台在 Agent 结束后从 Judge Pack 注入。

项目使用 Java 17、Maven 和 Spring Boot 4.0.4。基线代码可以编译，但每个目标都包含一个由对应任务要求修复的行为缺口。
