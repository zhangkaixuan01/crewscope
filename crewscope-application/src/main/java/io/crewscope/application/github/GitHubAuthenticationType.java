package io.crewscope.application.github;

/** Supported GitHub authentication shapes with distinct ownership semantics. */
public enum GitHubAuthenticationType {
    APP_INSTALLATION,
    OAUTH_USER
}
