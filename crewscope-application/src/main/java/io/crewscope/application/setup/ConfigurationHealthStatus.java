package io.crewscope.application.setup;

/** Stable status vocabulary for the derived configuration health projection. */
public enum ConfigurationHealthStatus {
    READY,
    ACTION_REQUIRED,
    BLOCKED,
    UNAVAILABLE
}
