package io.crewscope.application.artifact;

/** Sensitivity class applied independently from the artifact's visibility scope. */
public enum ArtifactDataClassification {
    PUBLIC,
    INTERNAL,
    CONFIDENTIAL,
    RESTRICTED
}
