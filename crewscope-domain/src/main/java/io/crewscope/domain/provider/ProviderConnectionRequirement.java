package io.crewscope.domain.provider;

/** Whether a ProviderImplementation executes natively or through an external Connection. */
public enum ProviderConnectionRequirement {
    NONE,
    REQUIRED
}
