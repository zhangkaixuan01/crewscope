package io.crewscope.domain.coding;

/** Business reason for finalizing a Workspace while preserving its delivery evidence. */
public enum ExecutionWorkspaceCompletionReason {
    SUCCEEDED,
    CANCELLED
}
