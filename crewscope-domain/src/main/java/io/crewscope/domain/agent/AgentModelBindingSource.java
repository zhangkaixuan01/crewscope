package io.crewscope.domain.agent;

/** Exact source from which an execution model pair was resolved. */
public enum AgentModelBindingSource {
    DIRECT,
    TEAM_DEFAULT,
    ORGANIZATION_DEFAULT
}
