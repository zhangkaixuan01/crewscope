package io.crewscope.domain.agent;

/** Stable runtime behavior shape of an Agent, independent from its business template. */
public enum AgentRuntimeRole {
    PERSONAL_ASSISTANT,
    TEAM_COORDINATOR,
    SPECIALIST
}
