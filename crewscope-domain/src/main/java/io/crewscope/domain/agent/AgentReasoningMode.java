package io.crewscope.domain.agent;

/** Provider-neutral reasoning preference accepted by the safe model adapter boundary. */
public enum AgentReasoningMode {
    DEFAULT,
    ENABLED,
    DISABLED
}
